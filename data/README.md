# Downloading Relevant Data

To download the relevant data first go create an entrez account and api key. Then
run the following code.

```
cd scripts
cp config.json.example config.json
```

Then in config.json change the entrez api key to be the api key that you have created through pubmed. This api key will be used
to generate the relevant pubmed data required for this.
