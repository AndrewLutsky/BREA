[project]
name = "brea"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AndrewLutsky", email = "andrew.lutsky19@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
brea = "brea:main"

[build-system]
requires = ["uv_build>=0.9.15,<0.10.0"]
build-backend = "uv_build"
